class EnergyModel{
  String title;
  String description;
  String amount;
  String bonus;
  String flashPoint;
  String point;
  bool isHotDeal;
  String purchaseId;

  EnergyModel(this.title, this.description, this.amount, this.bonus, this.flashPoint, this.point, this.isHotDeal, this.purchaseId);
}